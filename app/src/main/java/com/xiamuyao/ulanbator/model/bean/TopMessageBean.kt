package com.xiamuyao.ulanbator.model.bean

import androidx.databinding.BaseObservable

data class TopMessageBean(var id: Int, var topMessageTitle: String ,var topMessageUrl: String) : BaseObservable(){
}

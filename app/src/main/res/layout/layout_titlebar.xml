<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/titleBarLayout"
        android:layout_width="match_parent"
        android:background="@color/lineinfo"
        android:layout_height="44dp">

         <ImageView
            android:id="@+id/titleBarLeftImage"
            android:layout_width="@dimen/barLeftImg"
            android:layout_height= "@dimen/barLeftImg"
            android:layout_marginStart="15dp"
            android:background="@drawable/back_w"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="8dp"
            android:text="@string/titleBarLeftText"
            android:textColor="#000000"
            android:textSize="15sp"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/titleBarLeftImage"
            app:layout_constraintLeft_toRightOf="@id/titleBarLeftImage"
            app:layout_constraintTop_toTopOf="@id/titleBarLeftImage" />

        <TextView
            android:id="@+id/titleBarTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="20dp"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="title" />

        <ImageView
            android:id="@+id/titleBarRightImage"
            android:layout_width="@dimen/barRightImg"
            android:layout_height="@dimen/barRightImg"
            android:layout_marginEnd="8dp"
            android:background="#123"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/titleBarRightText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="10dp"
            android:textColor="#FFFFFF"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintRight_toLeftOf="@id/titleBarRightImage"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="登陆" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>